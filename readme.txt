Data for EDD

BEWARE! using .gitattributes.
Git does not handle UTF-16 nicely in diffs.
.act files can either be un-BOMed (plain ASCII) or BOMed (UTF-8, UTF-16-LE, UTF-16-BE) so putting in a
git attributes extension for that type is problematic.  best left alone


